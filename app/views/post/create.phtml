<?php use \Phalcon\Tag as Tag; ?>

<script type="text/javascript">
	$(document).ready(function() {
		$('#text').redactor({
			buttonSource: true,
			imageUpload: "<?= $this->url->get([ 'for' => 'post-upload' ]) ?>",
			plugins: ['video', 'readmore']
		});
	});
</script>

<?= Tag::form([ $this->url->get([ 'for' => 'post-create' ]), 'class' => 'form', 'data-ajax' => 'true' ]) ?>
	<div class="form-control">
		<label class="form-label" for="subject">Заголовок записи</label>
		<span class="form-help">Не больше 300 символов</span>
		<input type="text" name="post_subject" id="subject" placeholder="Заголовок записи" class="input">
	</div>
	
	<div class="form-control">
		<label class="form-label" for="slug">Алиас</label>
		<span class="form-help">Должен быть уникален</span>
		<input type="text" name="post_slug" id="slug" placeholder="Алиас" class="input">
	</div>
	
	<div class="form-control">
		<label class="form-label" for="text">Сообщение</label>
		<span class="form-help">Пиши нормально, блин!</span>
		<textarea name="post_text" cols="40" id="text" rows="10" placeholder="Сообщение"></textarea>
	</div>
	
	<div class="form-control">
		<label class="form-label" for="is_comment">Комментарии</label>
		<select name="post_is_comment" id="is_comment">
			<option value="true">Включены</option>
			<option value="false">Выключены</option>
		</select>
	</div>

	<hr>

	<div class="form-control">
		<label class="form-label" for="meta_description">Описание</label>
		<input type="text" name="post_meta_description" id="meta_description" placeholder="Обычно это текст до красной строки" class="input">
	</div>

	<div class="form-control">
		<label class="form-label" for="meta_keywords">Ключевые слова</label>
		<input type="text" name="post_meta_keywords" id="meta_keywords" placeholder="утка, нос, котопёс, веб, пушок, горшок" class="input">
	</div>

	<hr>

	<div class="form-control">
		<?= Tag::submitButton([ 'Добавить пост', 'name' => 'post_add', 'class' => 'btn' ]) ?>
		<?= Tag::submitButton([ 'Черновик', 'name' => 'post_draft', 'class' => 'btn btn-gray' ]) ?>
		<?= Tag::linkTo([
				$this->url->get([ 'for' => 'home-link' ]),
				'Отмена',
				'class' => 'btn btn-cancle'
			])
		?>
	</div>

<?= Tag::endForm() ?>